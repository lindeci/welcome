
``` cpp
struct handlerton {
    SHOW_COMP_OPTION state; // 显示组件选项
    legacy_db_type db_type; // 传统数据库类型
    uint slot; // 插槽
    uint savepoint_offset; // 保存点偏移量
    close_connection_t close_connection; // 关闭连接
    kill_connection_t kill_connection; // 终止连接
    pre_dd_shutdown_t pre_dd_shutdown; // 数据字典关机前回调
    reset_plugin_vars_t reset_plugin_vars; // 重置插件变量
    savepoint_set_t savepoint_set; // 设置保存点
    savepoint_rollback_t savepoint_rollback; // 回滚保存点
    savepoint_rollback_can_release_mdl_t savepoint_rollback_can_release_mdl; // 回滚保存点并释放锁
    savepoint_release_t savepoint_release; // 释放保存点
    commit_t commit; // 提交事务
    rollback_t rollback; // 回滚事务
    prepare_t prepare; // 准备事务
    recover_t recover; // 恢复事务
    recover_prepared_in_tc_t recover_prepared_in_tc; // 在TC中恢复预处理事务
    commit_by_xid_t commit_by_xid; // 根据XID提交事务
    rollback_by_xid_t rollback_by_xid; // 根据XID回滚事务
    set_prepared_in_tc_t set_prepared_in_tc; // 在TC中设置预处理事务
    set_prepared_in_tc_by_xid_t set_prepared_in_tc_by_xid; // 根据XID在TC中设置预处理事务
    create_t create; // 创建
    drop_database_t drop_database; // 删除数据库
    panic_t panic; // 紧急
    start_consistent_snapshot_t start_consistent_snapshot; // 开始一致的快照
    flush_logs_t flush_logs; // 刷新日志
    show_status_t show_status; // 显示状态
    partition_flags_t partition_flags; // 分区标志
    is_valid_tablespace_name_t is_valid_tablespace_name; // 是否是有效的表空间名
    get_tablespace_t get_tablespace; // 获取表空间
    alter_tablespace_t alter_tablespace; // 修改表空间
    get_tablespace_filename_ext_t get_tablespace_filename_ext; // 获取表空间文件名扩展
    upgrade_tablespace_t upgrade_tablespace; // 升级表空间
    upgrade_space_version_t upgrade_space_version; // 升级空间版本
    get_tablespace_type_t get_tablespace_type; // 获取表空间类型
    get_tablespace_type_by_name_t get_tablespace_type_by_name; // 根据名称获取表空间类型
    upgrade_logs_t upgrade_logs; // 升级日志
    finish_upgrade_t finish_upgrade; // 完成升级
    fill_is_table_t fill_is_table; // 填充IS表
    dict_init_t dict_init; // 数据字典初始化
    ddse_dict_init_t ddse_dict_init; // DDSE数据字典初始化
    dict_register_dd_table_id_t dict_register_dd_table_id; // 数据字典注册DD表ID
    dict_cache_reset_t dict_cache_reset; // 数据字典缓存重置
    dict_cache_reset_tables_and_tablespaces_t dict_cache_reset_tables_and_tablespaces; // 数据字典缓存重置表和表空间
    dict_recover_t dict_recover; // 数据字典恢复
    dict_get_server_version_t dict_get_server_version; // 获取数据字典服务器版本
    dict_set_server_version_t dict_set_server_version; // 设置数据字典服务器版本
    is_reserved_db_name_t is_reserved_db_name; // 是否是保留的数据库名
    uint32 flags; // 标志
    binlog_func_t binlog_func; // 二进制日志函数
    binlog_log_query_t binlog_log_query; // 二进制日志记录查询
    acl_notify_t acl_notify; // ACL通知
    discover_t discover; // 发现
    find_files_t find_files; // 查找文件
    table_exists_in_engine_t table_exists_in_engine; // 表在引擎中是否存在
    push_to_engine_t push_to_engine; // 推送到引擎
    is_supported_system_table_t is_supported_system_table; // 是否支持系统表
    sdi_create_t sdi_create; // SDI创建
    sdi_drop_t sdi_drop; // SDI删除
    sdi_get_keys_t sdi_get_keys; // 获取SDI键
    sdi_get_t sdi_get; // 获取SDI
    sdi_set_t sdi_set; // 设置SDI
    sdi_delete_t sdi_delete; // 删除SDI
    const char **file_extensions; // 文件扩展名
    is_dict_readonly_t is_dict_readonly; // 数据字典是否只读
    rm_tmp_tables_t rm_tmp_tables; // 删除临时表
    get_cost_constants_t get_cost_constants; // 获取成本常数
    replace_native_transaction_in_thd_t replace_native_transaction_in_thd; // 在线程中替换本地事务
    notify_exclusive_mdl_t notify_exclusive_mdl; // 通知独占MDL
    notify_alter_table_t notify_alter_table; // 通知修改表结构
    notify_rename_table_t notify_rename_table; // 通知重命名表
    notify_truncate_table_t notify_truncate_table; // 通知截断表
    rotate_encryption_master_key_t rotate_encryption_master_key; // 旋转加密主密钥
    redo_log_set_state_t redo_log_set_state; // 重做日志设置状态
    get_table_statistics_t get_table_statistics; // 获取表统计信息
    get_index_column_cardinality_t get_index_column_cardinality; // 获取索引列基数
    get_tablespace_statistics_t get_tablespace_statistics; // 获取表空间统计信息
    post_ddl_t post_ddl; // DDL后回调
    post_recover_t post_recover; // 恢复后回调
    Clone_interface_t clone_interface; // 克隆接口
    uint32 license; // 许可证
    void *data; // 数据
    lock_hton_log_t lock_hton_log; // 锁定HTON日志
    unlock_hton_log_t unlock_hton_log; // 解锁HTON日志
    collect_hton_log_info_t collect_hton_log_info; // 收集HTON日志信息
    uint32 foreign_keys_flags; // 外键标志
    check_fk_column_compat_t check_fk_column_compat; // 检查外键列兼容性
    LEX_CSTRING fk_name_suffix; // 外键名称后缀
    prepare_secondary_engine_t prepare_secondary_engine; // 准备次要引擎
    optimize_secondary_engine_t optimize_secondary_engine; // 优化次要引擎
    compare_secondary_engine_cost_t compare_secondary_engine_cost; // 比较次要引擎成本
    SecondaryEngineFlags secondary_engine_flags; // 次要引擎标志
    secondary_engine_modify_access_path_cost_t secondary_engine_modify_access_path_cost; // 修改次要引擎访问路径成本
    get_secondary_engine_offload_or_exec_fail_reason_t get_secondary_engine_offload_or_exec_fail_reason; // 获取次要引擎卸载或执行失败原因
    set_secondary_engine_offload_fail_reason_t set_secondary_engine_offload_fail_reason; // 设置次要引擎卸载失败原因
    secondary_engine_check_optimizer_request_t secondary_engine_check_optimizer_request; // 次要引擎检查优化器请求
    se_before_commit_t se_before_commit; // 提交前次要引擎回调
    se_after_commit_t se_after_commit; // 提交后次要引擎回调
    se_before_rollback_t se_before_rollback; // 回滚前次要引擎回调
    notify_after_select_t notify_after_select; // SELECT后通知
    notify_create_table_t notify_create_table; // 创建表通知
    notify_drop_table_t notify_drop_table; // 删除表通知
    Page_track_t page_track; // 页面跟踪
}
```